# Webhook 通知推送服务
#
# 快速启动:
#   1. mkdir -p hooks notify && touch .env
#   2. 编辑 .env，填入通知平台的 Webhook 地址 / Token（不需要的平台留空）
#   3. docker compose up -d
#
# 服务启动后访问控制台: http://localhost:9000/ui
# 各平台通知地址:
#   飞书:     http://localhost:9000/hooks/feishu?msg=Hello
#   钉钉:     http://localhost:9000/hooks/dingtalk?msg=Hello
#   企业微信:  http://localhost:9000/hooks/wecom?msg=Hello
#   Telegram: http://localhost:9000/hooks/telegram?msg=Hello
#   Slack:    http://localhost:9000/hooks/slack?msg=Hello
#   Discord:  http://localhost:9000/hooks/discord?msg=Hello
#   Server酱: http://localhost:9000/hooks/serverchan?msg=Hello&title=标题

services:
  webhook:
    image: ghcr.io/always1ov/webhook:latest
    restart: unless-stopped
    ports:
      - "9000:9000"
    env_file:
      - path: .env
        required: false
    environment:
      HOST: "0.0.0.0"
      PORT: "9000"
      HOT_RELOAD: "true"
      VERBOSE: "true"
      HOOKS_DIR: "/hooks"
      LANGUAGE: "zh-CN"
      WEBUI: "true"
    volumes:
      - ./hooks:/hooks:ro
      - ./notify:/notify
    entrypoint: ["/bin/sh", "-c", "find /notify -name '*.sh' -exec chmod +x {} \\; 2>/dev/null; exec /usr/bin/webhook"]
